# U8 Deployment Runbook — AWS Phase 1

> Phase 1: 6개월 AWS 크레딧 활용 (~$200). EC2 t4g.small + Cloudflare Proxy 조합으로 월 비용 ~$0.
> 도메인: `minya.life`. 호스팅 후 [data/devlog/](../data/devlog/) 에 회고 항목 추가 예정.

## 사전 준비물 체크리스트

| 항목 | 상태 |
|---|---|
| AWS 계정 (신규, 크레딧 $100~$200 활성화) | ☐ |
| 결제 카드 등록 (Free Tier 가입 필수) | ☐ |
| 도메인 `minya.life` (가비아·hosting.kr 등에서 1년 3,000원) | ☐ |
| Cloudflare 계정 (무료 플랜) | ☐ |
| GitHub 저장소 (이 프로젝트 push 완료) | ☐ |
| 로컬 SSH 키 쌍 생성 (`ssh-keygen -t ed25519`) | ☐ |

---

## Step 1 — 도메인 + Cloudflare DNS 연결 (30분)

1. 레지스트라(가비아 등)에서 `minya.life` 구매
2. Cloudflare 가입 → **Add Site** → `minya.life` 입력 → Free 플랜 선택
3. Cloudflare 가 제시하는 nameserver 2개를 레지스트라에 등록
   - 가비아: 마이페이지 → 도메인 관리 → 네임서버 변경
4. DNS 전파 확인: `nslookup -type=ns minya.life` 가 Cloudflare ns 반환할 때까지 (보통 1~24시간)

---

## Step 2 — AWS 계정 + EC2 인스턴스 (90~120분)

처음 AWS 작업 기준 시간. 익숙해지면 30분.

### 2.0 사전 — SSH 키 페어 생성 (5분, EC2 만들기 전)

EC2 접속에 쓸 키 페어를 **로컬에서 먼저** 만들고 public 부분만 AWS 에 등록한다. PowerShell:

```powershell
# Windows 10/11 은 OpenSSH 기본 포함
ssh-keygen -t ed25519 -f $env:USERPROFILE\.ssh\livelab -C "livelab@minya.life"
# Enter 2번 (passphrase 생략 가능 — 운영에선 권장)
# 결과:
#   C:\Users\MINYA\.ssh\livelab        ← private key (절대 공유 금지)
#   C:\Users\MINYA\.ssh\livelab.pub    ← public key (AWS에 등록)
```

권한 설정:

⚠ **머신 SID 함정 — 컴퓨터명 == 사용자명 이면 아래 `icacls` 명령은 실패합니다.**
`${env:USERNAME}` 이 `MINYA` 일 때 컴퓨터명도 `MINYA` 면, Windows 가 *머신 계정* 으로 먼저 해석해 키에 *머신 SID* 가 붙습니다. OpenSSH 가 *"Bad permissions"* 로 거부하고 들어가지 못함. 회고: [data/ops/incident/01-ssh-key-machine-sid.md](../data/ops/incident/01-ssh-key-machine-sid.md).

**안전한 길 — .NET ACL API 로 본인 SID 직접 grant** (어떤 환경에서도 동일하게 동작):

```powershell
$path = "$env:USERPROFILE\.ssh\livelab"
$user = [System.Security.Principal.WindowsIdentity]::GetCurrent().User
$acl  = Get-Acl $path
$acl.SetAccessRuleProtection($true, $false)              # inheritance 끔
$acl.Access | ForEach-Object { [void]$acl.RemoveAccessRule($_) }   # 모든 기존 ACE 제거
$rule = New-Object System.Security.AccessControl.FileSystemAccessRule($user, "Read", "Allow")
$acl.SetAccessRule($rule)
Set-Acl -Path $path -AclObject $acl

# 확인: 본인 SID 한 줄만 보이고 'MINYA\MINYA' 같은 머신 계정 표시 없어야 함
icacls $path
```

(`icacls /grant:r "${env:USERNAME}:R"` 도 컴퓨터명과 사용자명이 다르면 동작합니다. 이름이 같은 환경에서만 위 .NET 방식이 필수.)

public key 내용 확인 — Step 2.4 에서 사용:
```powershell
Get-Content $env:USERPROFILE\.ssh\livelab.pub
# 출력: ssh-ed25519 AAAAC3... livelab@minya.life
```

### 2.1 AWS 계정 생성 (15~30분, 카드 검증 변수)

1. <https://aws.amazon.com/ko/> → **AWS 계정 생성 (Create an AWS Account)**
2. 입력 정보:
   - **Email** — minya8703@gmail.com 권장 (도메인 일관성)
   - **AWS 계정 이름** — 예: `minya-livelab`
   - **암호** — 강한 암호
3. **연락처 정보**:
   - 계정 유형: **개인 (Personal)**
   - 한국 주소 영문 입력 (도로명 주소 영문 표기 — <https://www.juso.go.kr> 에서 영문 변환 가능)
4. **결제 정보**:
   - 한국 카드 종종 거절됨. 우선순위:
     1. **VISA/Mastercard 해외 결제 가능 신용/체크카드**
     2. 거절 시 카카오뱅크/토스 해외결제 가능 카드
     3. 그래도 거절되면 카드사에 "AWS 등록을 위한 해외 결제 차단 풀어달라" 요청
   - 본인 인증 시 $1 임시 청구 → 자동 환불
5. **전화 인증** — SMS 또는 음성 인증 코드
6. **Support Plan**: **Basic (무료)** 선택
7. 계정 활성화 후 환영 메일 도착까지 5~30분

**Free Tier 크레딧 확인**:
- 가입 직후 자동 활성화 (별도 신청 불필요)
- 콘솔 우상단 본인 이름 클릭 → **Billing and Cost Management** → 좌측 **Credits** → "AWS Free Tier" 또는 "Welcome Credit" $100 이상 표시되어야 정상

### 2.2 보안 잠금 — MFA 활성화 + IAM 사용자 (15분)

**root 계정 보호 = 가장 중요한 첫 작업.** root 가 털리면 크레딧 전부 날아갈 수 있음.

#### 2.2.1 Root 에 MFA 추가
1. 콘솔 우상단 본인 이름 → **Security credentials**
2. **Multi-factor authentication (MFA)** → **Assign MFA device**
3. 디바이스 이름: `root-mfa`
4. 유형: **Authenticator app** 선택 (Google Authenticator / Microsoft Authenticator / 1Password 등)
5. QR 스캔 → 연속 코드 2개 입력 → 등록 완료

#### 2.2.2 작업용 IAM 사용자 생성
일상 작업은 root 가 아닌 IAM 사용자로:

1. 콘솔 검색창 → `IAM` → IAM 서비스 열기
2. 좌측 **Users** → **Create user**
3. **Step 1**: User name 입력 — `livelab-admin`
4. **Provide user access to the AWS Management Console** ✓ 체크
   - User type: **I want to create an IAM user**
   - Console password: **Autogenerated password** 또는 직접 입력
   - Users must create a new password at next sign-in: **체크 해제**
5. **Step 2 Permissions**:
   - **Attach policies directly** 선택
   - 검색: `AdministratorAccess` → 체크
   - (보안 강화 원하면 더 좁은 policy 만들 수도 있지만 이번엔 단순화)
6. **Step 3 Review** → **Create user**
7. 다음 화면에서 **.csv download** 클릭 → 비밀번호와 콘솔 URL 안전한 곳 저장

#### 2.2.3 Access Key 발급 (CLI 용)
1. 방금 만든 `livelab-admin` 클릭 → **Security credentials** 탭
2. **Access keys** 섹션 → **Create access key**
3. Use case: **Command Line Interface (CLI)** 선택 → 다음 → confirmation 체크 → 다음
4. (선택) Description: `local-dev`
5. **Create access key** → **.csv download** 즉시 (다시 못 봄)
   - Access key ID: AKIAxxx...
   - Secret access key: 한 번만 표시됨

#### 2.2.4 IAM 사용자 로그아웃 → IAM 사용자로 재로그인
이후 작업은 **root 가 아닌 livelab-admin 으로**:
1. 우상단 본인 이름 → **Sign out**
2. .csv 의 콘솔 sign-in URL 로 로그인
3. IAM 사용자도 MFA 활성화 권장 (동일 절차)

### 2.3 AWS CLI 설치 + 설정 (10분, 선택)

EC2 만들기는 콘솔로 충분하지만, CLI 가 있으면 budget·검증 명령이 편함.

```powershell
# 설치
winget install Amazon.AWSCLI
# 또는
msiexec.exe /i https://awscli.amazonaws.com/AWSCLIV2.msi

# 새 PowerShell 창 열어서 확인
aws --version

# 자격증명 설정
aws configure
# AWS Access Key ID: <2.2.3 의 Access key ID>
# AWS Secret Access Key: <2.2.3 의 Secret>
# Default region name: ap-northeast-2     (Seoul) 또는 ap-northeast-1 (Tokyo)
# Default output format: json

# 검증 — 본인 IAM 사용자 정보 반환되면 OK
aws sts get-caller-identity
```

### 2.4 리전 선택 + 콘솔 전환 (2분)

- **Seoul (ap-northeast-2)** 한국 사용자 latency 최소. 추천.
- Tokyo (ap-northeast-1) 도 OK. 가격 동일.
- 콘솔 우상단 리전 드롭다운에서 선택 → **이후 모든 작업이 이 리전에서**

⚠ **주의**: 리전을 잘못 전환하면 만든 리소스가 "안 보임" → 다른 리전이라 그런 것. 항상 우상단 확인.

### 2.5 EC2 인스턴스 launch (20~30분)

#### 2.5.1 EC2 콘솔 열기
1. 콘솔 검색 → `EC2` → EC2 서비스
2. 좌측 **Instances** → **Launch instances** (주황 버튼)

#### 2.5.2 Name and tags
- Name: `livelab-app-1`

#### 2.5.3 Application and OS Images (AMI)
1. **Amazon Linux** 탭
2. AMI: **Amazon Linux 2023 AMI** (디폴트)
3. Architecture 드롭다운: **64-bit (Arm)** 선택 ← 중요! t4g 는 ARM
4. AMI ID 가 `ami-xxx (64-bit (Arm))` 으로 표시되어야 함

#### 2.5.4 Instance type
- 검색: `t4g.small`
- 선택: **t4g.small** (2 vCPU · 2 GiB · ARM Graviton)
- ⚠ Free Tier 표시 없음 — t4g.small 은 Free Tier 대상 아니라 **크레딧으로 결제**됨 (시간당 $0.0168, 월 ~$12)
- Free Tier 만 쓰고 싶으면 `t4g.micro` 가능하나 1 GiB RAM 으로 우리 스택 빡빡

#### 2.5.5 Key pair (login)
1. **Create new key pair** 클릭하지 말고 → **Use existing key pair** 가 없으면 다음 절차:
2. **Import key pair** 옵션 찾기 (또는 EC2 좌측 메뉴 → Key Pairs → Actions → Import key pair)
3. Name: `livelab`
4. Public key 붙여넣기: **Step 2.0 에서 만든 `livelab.pub` 내용 전체** (`ssh-ed25519 AAAA...`)
5. Import → EC2 launch wizard 로 돌아와서 방금 만든 `livelab` 선택

#### 2.5.6 Network settings
- VPC: **default** (별도 VPC 만들 필요 없음)
- Subnet: **No preference** (아무 AZ)
- Auto-assign public IP: **Enable** ← 중요
- **Firewall (security groups)**:
  - **Create security group** 선택
  - Security group name: `livelab-sg`
  - Description: `Live Lab — SSH from my IP, HTTP/HTTPS from Cloudflare`
  - **인바운드 규칙** (Add security group rule 로 추가):

| Type | Port | Source |
|---|---|---|
| SSH | 22 | **My IP** (콘솔이 자동 감지) |
| HTTP | 80 | `0.0.0.0/0` *(임시 — Cloudflare 연결 후 좁힘)* |
| HTTPS | 443 | `0.0.0.0/0` *(임시)* |

8089 포트는 **추가하지 않음** (Cloudflare 가 80/443 받아서 EC2 내부에서만 8089 사용)

#### 2.5.7 Configure storage
- 1x 30 GiB **gp3** (Free Tier 한도 안)
- 그대로 두기

#### 2.5.8 Advanced details — User data ★

페이지 가장 아래 **Advanced details** 펼침 → 스크롤 거의 끝까지 → **User data**:

1. 로컬에서 [`infra/aws/user-data.sh`](../infra/aws/user-data.sh) 내용 전체 복사
2. User data 텍스트 박스에 붙여넣기
3. Base64 인코딩 옵션은 **체크 해제** (그대로 plain text)

#### 2.5.9 Launch
- 우측 패널 Summary 확인 → 비용 예상치 표시
- **Launch instance** 클릭
- 성공 화면 → **View all instances**

### 2.6 인스턴스 기동 + Public IP 확인 (2분)

1. Instances 목록에 `livelab-app-1` 표시
2. State: **Pending → Running** (1~2분)
3. Status checks: **2/2 checks passed** 표시까지 추가 1~2분
4. 인스턴스 선택 → 하단 Details 탭:
   - **Public IPv4 address**: 예 `54.180.x.x` ← 메모. Cloudflare DNS에 입력할 값.

### 2.7 SSH 첫 접속 (3분)

PowerShell 에서:
```powershell
ssh -i $env:USERPROFILE\.ssh\livelab ec2-user@<EC2_PUBLIC_IP>
# 첫 접속 시 host fingerprint 확인 → yes
```

성공 시 motd 출력:
```
  민야령 Backend Live Lab — EC2 부트스트랩 완료.
  ...
```

❌ 안 되면 점검:
- 보안 그룹 SSH 22 인바운드에 내 IP 가 정확히 있는지 (집·회사·카페 IP 다 다름)
- 키 파일 권한 (`icacls` 다시)
- 사용자명이 `ec2-user` (Amazon Linux 기본)

### 2.8 User data 실행 확인

EC2 안에서:
```bash
sudo cat /var/log/livelab-bootstrap.log
# 마지막에 "bootstrap complete at ..." 출력되면 정상

# Docker 동작 확인
docker --version
docker compose version
# 둘 다 정상 출력되어야 함

# (선택) ec2-user 가 sudo 없이 docker 쓰는지 — 처음엔 그룹 적용 안 되어 있을 수 있음
docker ps
# permission denied 나면 한 번 로그아웃 → 재로그인:
exit
ssh -i $env:USERPROFILE\.ssh\livelab ec2-user@<EC2_PUBLIC_IP>
docker ps   # 이번엔 OK
```

여기까지 성공하면 Step 2 완료 — Step 3 (코드 클론 + 배포) 로 진행.

---

### Step 2 자주 막히는 곳 요약

| 증상 | 원인 | 해결 |
|---|---|---|
| 가입 시 카드 거절 | 한국 카드 해외결제 차단 | 카드사 콜센터에 "해외 결제 허용" 요청 |
| 가입 후 EC2 launch 가 *"verification pending"* | AWS 가입 직후 1~24시간 검증 대기 | 기다림. 그동안 도메인·Cloudflare 작업 병행 |
| ARM 표시 안 보임 | x86 AMI 선택함 | Architecture 드롭다운에서 **Arm** 명시 선택 |
| SSH "Permission denied (publickey)" | 키 권한 또는 사용자명 오류 | `icacls /inheritance:r /grant:r "${env:USERNAME}:R"`, 사용자명은 `ec2-user` |
| SSH "Connection timed out" | 보안 그룹 SSH 22 가 내 IP 외 차단 | 보안 그룹 → 인바운드 → SSH 의 Source 를 **My IP** 로 갱신 |
| user-data 효과 없음 | 콘솔에서 인스턴스 launch 후 user-data 수정함 | user-data 는 **첫 부팅 시 1회만** 실행. 인스턴스 종료 → 재생성 필요 |

---

## Step 3 — 첫 배포 (30분)

### 3.1 SSH 접속

```powershell
# Windows PowerShell
ssh -i $env:USERPROFILE\.ssh\livelab ec2-user@<EC2_PUBLIC_IP>

# macOS / Linux 면 ~ 경로 그대로
# ssh -i ~/.ssh/livelab ec2-user@<EC2_PUBLIC_IP>
```

⚠ **키 파일명 일관성** — Step 2.0 에서 `ssh-keygen` 으로 만들었으면 확장자 없는 `livelab`. AWS 콘솔에서 키페어를 *다운로드*받으면 `.pem` 확장자가 붙어 `livelab.pem` 이 됩니다. 본인 환경에 맞는 파일명으로 `-i` 인자 조정.

`/etc/motd` 에 다음 단계 안내가 보이면 user-data 정상 실행 완료.

### 3.2 코드 클론 + 환경 변수
```bash
cd ~
git clone https://github.com/<USERNAME>/live-lab.git
cd live-lab
cp .env.example .env
vi .env
# 다음 두 값을 채움:
# GOOGLE_API_KEY=AIza...
# POSTGRES_PASSWORD=<강한 비밀번호>
```

### 3.3 서비스 기동
```bash
docker compose --profile prod up -d
docker compose ps   # 모든 컨테이너 healthy 확인
docker compose logs -f app   # 부팅 로그 (Tomcat started on port 8089 확인 후 Ctrl+C)
```

### 3.4 EC2 안에서 로컬 테스트 (5분)

`docker compose logs -f app` 에서 `Tomcat started on port 8089` 확인되면 Ctrl+C 로 빠져나온 다음, 같은 EC2 안에서 (Cloudflare 거치지 않고) 직접 호출:

```bash
# 1. 헬스 체크 — Spring Boot Actuator
curl -i http://localhost:8089/actuator/health
# 기대 출력:
#   HTTP/1.1 200
#   Content-Type: application/vnd.spring-boot.actuator.v3+json
#   {"status":"UP"}

# 2. 우리 API
curl -i http://localhost:8089/api/status
# 기대 출력:
#   HTTP/1.1 200
#   Content-Type: application/json;charset=UTF-8
#   {"currentUnit":8,"totalUnits":11,"currentLabel":"U8 ...","units":{...}}

# 3. 랜딩 페이지 (HTML)
curl -i http://localhost:8089/ | head -20
# 기대: HTML 시작 `<!doctype html>` ...

# 4. 챗봇·Redis·Kafka 데모 페이지 존재 확인 (상태 200 만 보면 됨)
for path in / /lab/chat.html /lab/redis.html /lab/kafka.html /lab/devlog.html /lab/ops.html; do
  echo -n "$path → "
  curl -o /dev/null -s -w "%{http_code}\n" http://localhost:8089$path
done
# 6개 모두 200 이면 정상
```

#### 흔한 에러와 해결

| 증상 | 원인 | 해결 |
|---|---|---|
| `docker compose build` 가 *"buildx component ... version 0.17 required"* 로 거부 | AL2023 의 `dnf install docker` 가 깔아주는 buildx 가 0.17 미만 | EC2 안에서 buildx 0.17.1 수동 설치 (아래 박스). user-data.sh 4.5 단계가 이미 처리하지만 옛 인스턴스 / user-data 미실행 환경 대비 |
| `curl: (7) Failed to connect to localhost port 8089: Connection refused` | Spring 앱이 아직 부팅 안 됨 / 종료됨 | `docker compose ps` → `app` 상태 확인. `Exited` 이면 `docker compose logs app` 으로 원인 |
| `200` 인데 응답이 빈 HTML | 정적 파일이 컨테이너에 안 들어감 | `Dockerfile` 의 `COPY` 라인 점검 → 이미지 재빌드 (`docker compose build app && docker compose up -d app`) |
| `503 Service Unavailable` | DB 또는 Redis 미기동 | `docker compose ps` 에서 postgres/redis healthy 확인. 시간 더 필요할 수 있음 (Kafka 30초 정도) |
| `502 Bad Gateway` | Spring 부팅 중 OOM 으로 죽었다 살아남 | `docker compose logs app --tail 100` 에서 `OutOfMemoryError` 검색. t4g.small 2GB 빡빡 — JVM heap (`MaxRAMPercentage`) + Kafka heap 축소 |

**buildx 0.17.1 수동 설치 (EC2 안):**

```bash
BUILDX_VERSION=v0.17.1   # 명시 고정 — 'latest' 를 grep 으로 파싱하면 빈 변수로 404 나는 함정
sudo mkdir -p /usr/local/lib/docker/cli-plugins
sudo curl -fsSL "https://github.com/docker/buildx/releases/download/${BUILDX_VERSION}/buildx-${BUILDX_VERSION}.linux-arm64" \
  -o /usr/local/lib/docker/cli-plugins/docker-buildx
sudo chmod +x /usr/local/lib/docker/cli-plugins/docker-buildx
docker buildx version   # v0.17.1 보여야 성공
```

### 3.5 PC 에서 Public IP 로 한 번 더 (선택)

EC2 보안 그룹의 80/443 이 일시적으로 `0.0.0.0/0` 열려있는 동안이라면, PC 브라우저에서도 확인 가능:

```
http://<EC2_PUBLIC_IP>:8089/api/status
```

8089 가 보안 그룹에서 비공개라면 이 단계는 건너뛰고 바로 Step 4 (Cloudflare) 로. Cloudflare 연결 후엔 도메인으로만 접근하면 됩니다.

---

## Step 4 — Cloudflare DNS + HTTPS 연결 (15~20분)

Cloudflare 가 사용자 ↔ EC2 사이에서 HTTPS·CDN·DDoS 보호를 무료로 처리합니다. 사용자는 `https://minya.life` 만 보고, EC2 는 그대로 HTTP 8089 만 노출하면 됩니다.

### 4.0 사전 — Cloudflare nameserver 가 이미 적용됐는지 확인

Step 1 에서 도메인 nameserver 를 Cloudflare 로 옮기셨죠. 적용 됐는지:

```bash
# EC2 안에서 또는 PowerShell 에서
nslookup -type=ns minya.life
# 출력에 *.ns.cloudflare.com 두 개가 보여야 정상
# 아직 옛 레지스트라 nameserver 가 나오면 1~24시간 더 대기
```

전파 끝나지 않은 상태에서 다음 단계 진행해도 Cloudflare 측 설정은 미리 다 해둘 수 있고, 전파 완료 즉시 자동 활성화됩니다.

### 4.1 DNS 레코드 추가 (5분)

1. **Cloudflare 대시보드 로그인** → 좌측 사이트 목록에서 `minya.life` 클릭
2. 좌측 메뉴 **DNS** → **Records**
3. 기본으로 들어가있는 레코드(예: `parking` MX 등)가 있으면 **Edit → Delete** 로 정리
4. **+ Add record** 클릭

#### 첫 번째 레코드 — 루트 도메인

| 필드 | 값 | 설명 |
|---|---|---|
| Type | `A` | IPv4 주소 매핑 |
| Name | `@` | `@` 는 *루트 도메인* 즉 `minya.life` 자체. Cloudflare 가 입력 후 알아서 표시 |
| IPv4 address | `<EC2 Public IP>` | 예: `15.134.131.69`. **Elastic IP 안 붙이면 인스턴스 재시작 시마다 바뀜** |
| Proxy status | **🟠 Proxied** (구름 아이콘 주황색) | 핵심 — 회색은 DNS 만, 주황색은 Cloudflare 경유 (HTTPS·CDN 활성화) |
| TTL | **Auto** | Proxied 면 자동 |

→ **Save**

#### 두 번째 레코드 — www 서브도메인

같은 방식으로 한 번 더:

| 필드 | 값 |
|---|---|
| Type | `A` |
| Name | `www` |
| IPv4 address | `<EC2 Public IP>` (위와 동일) |
| Proxy status | 🟠 Proxied |
| TTL | Auto |

→ **Save**

이제 `minya.life` 와 `www.minya.life` 둘 다 EC2 로 향함.

#### 결과 확인

```bash
# 1~5분 대기 후
nslookup minya.life
# 출력에 Cloudflare IP (104.x.x.x 또는 172.67.x.x) 가 보이면 정상
# 직접 EC2 IP 가 보이면 Proxy 가 회색(off) 상태 — Cloudflare 보호 안 받음
```

### 4.2 SSL/TLS 모드 설정 (3분)

좌측 메뉴 **SSL/TLS** → **Overview**:

표시되는 4가지 옵션:

| 모드 | 사용자 ↔ Cloudflare | Cloudflare ↔ EC2 | 권장 |
|---|---|---|---|
| Off | HTTP | HTTP | ❌ 절대 X |
| **Flexible** | HTTPS | HTTP | ✅ **첫 셋업 권장** — EC2 에 인증서 불필요 |
| Full | HTTPS | HTTPS (자체 서명도 OK) | 한 단계 강화 시 |
| Full (strict) | HTTPS | HTTPS (신뢰 가능 CA 인증서 필수) | 운영 안정 후 |

→ **Flexible** 선택 (라디오 버튼 클릭, 자동 저장)

⚠ Full (strict) 로 하려면 EC2 에 Let's Encrypt 등 인증서를 따로 깔아야 합니다. 우리는 EC2 가 HTTP 8089 그대로라서 Flexible 이 가장 자연스러움.

### 4.3 강제 HTTPS + 추가 설정 (5분)

#### Edge Certificates 자동 발급 확인

좌측 **SSL/TLS → Edge Certificates** 페이지에서:

- **Universal SSL** 섹션 → Status: **Active** 보여야 함 (보통 자동 발급, 15분~24시간 걸릴 수 있음)
- 발급 끝나기 전엔 https://minya.life 가 인증서 에러로 보임 — 기다림

#### Always Use HTTPS 활성화

같은 페이지 **Edge Certificates** 하단:

- **Always Use HTTPS** 토글 → **On**
- 효과: `http://minya.life` 접근 시 자동으로 `https://minya.life` 로 리디렉트

#### Automatic HTTPS Rewrites (선택)

- 효과: HTML 안의 `http://` 링크를 자동으로 `https://` 로 다시 씀 (혼합 콘텐츠 경고 회피)
- 토글 **On** 권장

#### Minimum TLS Version

- **TLS 1.2** 이상 권장 (구식 클라이언트 차단)
- **SSL/TLS → Edge Certificates → Minimum TLS Version → 1.2** 선택

### 4.4 EC2 보안 그룹 좁히기 (5분, 선택이지만 권장)

이제 Cloudflare 가 80/443 을 받으니, EC2 의 보안 그룹 인바운드를 **Cloudflare IP 만** 으로 좁혀 직접 접근을 차단:

1. AWS 콘솔 **EC2 → Security Groups → `livelab-sg`** 클릭
2. **Inbound rules → Edit inbound rules**
3. 기존 HTTP/HTTPS 규칙 (Source `0.0.0.0/0`) 삭제 후 다시 추가:

| Type | Port | Source |
|---|---|---|
| HTTP | 80 | `173.245.48.0/20` (Cloudflare) |
| HTTP | 80 | `103.21.244.0/22` |
| HTTP | 80 | `103.22.200.0/22` |
| HTTP | 80 | `103.31.4.0/22` |
| HTTP | 80 | `141.101.64.0/18` |
| HTTP | 80 | `108.162.192.0/18` |
| HTTP | 80 | `190.93.240.0/20` |
| HTTP | 80 | `188.114.96.0/20` |
| HTTP | 80 | `197.234.240.0/22` |
| HTTP | 80 | `198.41.128.0/17` |
| HTTP | 80 | `162.158.0.0/15` |
| HTTP | 80 | `104.16.0.0/13` |
| HTTP | 80 | `104.24.0.0/14` |
| HTTP | 80 | `172.64.0.0/13` |
| HTTP | 80 | `131.0.72.0/22` |
| HTTPS | 443 | (HTTP 80 과 동일한 15개 범위) |

전체 최신 목록: <https://www.cloudflare.com/ips-v4>

⚠ 30개 규칙 직접 입력은 부담스러우니 초기엔 `0.0.0.0/0` 그대로 두고, 사이트 안정화 후 좁혀도 OK. Cloudflare 통과 정책은 Cloudflare WAF 가 1차 방어.

### 4.5 동작 검증 (3분)

```bash
# DNS 가 Cloudflare 로 가는지
curl -I https://minya.life
# 기대 출력:
#   HTTP/2 200
#   server: cloudflare              ← 핵심: Cloudflare 거쳐서 옴
#   cf-cache-status: DYNAMIC
#   content-type: text/html;charset=UTF-8

# API 도 확인
curl https://minya.life/api/status | head
# JSON 응답 보이면 끝
```

브라우저에서도 <https://minya.life> 열어보고:
- 자물쇠 아이콘이 보여야 함
- 인증서 → 발급자: **Cloudflare** 또는 **Google Trust Services** 등 (Universal SSL 발급자)
- 페이지 정상 로드, 한글 깨짐 없음

### 4.6 자주 막히는 곳

| 증상 | 원인 | 해결 |
|---|---|---|
| 브라우저: "이 사이트에 연결할 수 없음" | Cloudflare nameserver 전파 미완 | `nslookup -type=ns minya.life` 가 `*.ns.cloudflare.com` 보일 때까지 대기 |
| Error 1016 / Origin DNS error | DNS A 레코드 IP 오타 | DNS Records 에서 IP 다시 확인 |
| Error 521 (Web server is down) | EC2 가 80 port 닫힘 또는 앱 꺼짐 | EC2 안에서 `curl localhost:8089` 부터 통과하는지. 보안 그룹 80 인바운드 열림 확인 |
| Error 522 (Connection timed out) | 보안 그룹이 Cloudflare IP 차단 | 임시로 80/443 Source 를 `0.0.0.0/0` 으로 다시 열고 4.4 좁히기는 나중에 |
| Error 525 (SSL handshake failed) | SSL/TLS 모드 Full 인데 EC2 에 인증서 없음 | **Flexible** 로 변경 |
| `http://` 가 `https://` 로 리디렉트 안 됨 | Always Use HTTPS 꺼져있음 | 4.3 의 토글 켜기 |
| 한글이 mojibake | Cloudflare 가 charset 안 넘김 (드물) | Page Rules 에서 해당 도메인 *"Disable Performance"* 또는 Workers 캐시 우회 |

여기까지 완료되면 사이트가 외부에서 정상 접속 가능 — Step 5 (비용 가드레일) 로 진행.

테스트:
```bash
curl -i https://minya.life
curl -i https://minya.life/api/status
```

---

## Step 5 — 비용 가드레일 (15분)

### 5.1 Budget 알람 ($15/mo 상한)
[infra/aws/budget.json](../infra/aws/budget.json) + [infra/aws/budget-notifications.json](../infra/aws/budget-notifications.json) 사용:

```bash
aws budgets create-budget \
  --account-id <ACCOUNT_ID> \
  --budget file://infra/aws/budget.json \
  --notifications-with-subscribers file://infra/aws/budget-notifications.json
```

이메일 확인 메일 들어오면 승인.

### 5.2 Free Tier 사용량 알람
AWS Console → Billing → Free Tier alerts → Enable.

### 5.3 일일 비용 확인 습관
```bash
aws ce get-cost-and-usage \
  --time-period Start=$(date -u +%Y-%m-01),End=$(date -u +%Y-%m-%d) \
  --granularity DAILY \
  --metrics UnblendedCost
```

---

## Step 6 — 추가 AWS 서비스 데모 (선택, U10 콘텐츠용)

크레딧 6개월 안에 직접 사용 경험을 만들 서비스:

- **S3**: 정적 자산(이미지·CSS) 업로드 또는 로그 백업
- **CloudFront**: S3 앞에 CDN 붙여 글로벌 캐싱
- **Lambda**: 매일 1회 데모 데이터 리프레시 (Java/Python)
- **CloudWatch Logs**: EC2 의 docker logs 를 AWS 측에 수집

각각 사용 시 [data/devlog/](../data/devlog/) 에 회고 항목 1개씩 추가.

---

## 트러블슈팅

### 컨테이너 OOM 으로 죽음
t4g.small 은 2GB RAM. Spring + Postgres + Redis + Kafka 동시 실행이 빡빡.

대응:
- Spring JVM heap 명시: `-XX:MaxRAMPercentage=50.0` + `mem_limit: 700m`
- Kafka heap 줄임: `KAFKA_HEAP_OPTS=-Xmx384m -Xms256m`
- swap 2GB 추가 (응급 보험)
- 그래도 부족하면 t4g.medium 으로 (월 ~$24)

### Cloudflare 가 503 반환
- EC2 보안 그룹에서 Cloudflare IP 범위 인바운드 허용 확인
- EC2 의 `nginx`/`Spring Boot` 가 80 또는 443에서 듣는지 확인
- Cloudflare DNS A 레코드의 IP 가 현재 EC2 IP 인지 (재시작 시 IP 바뀜 — Elastic IP 권장)

### Elastic IP 할당
EC2 재시작 시 public IP 변경됨 → Cloudflare DNS 매번 수정 부담. Elastic IP 할당:
```bash
aws ec2 allocate-address --domain vpc
aws ec2 associate-address --instance-id <INSTANCE_ID> --allocation-id <ALLOC_ID>
```
주의: Elastic IP 가 인스턴스에 연결되지 **않은** 상태로 두면 시간당 $0.005 과금.

---

## 완료 기준

- [ ] https://minya.life 로 사이트 접속 가능
- [ ] /lab/chat · /lab/redis · /lab/kafka · /lab/devlog · /lab/ops 모두 동작
- [ ] AWS Budget 알람 활성화
- [ ] data/devlog/ 에 "AWS Phase 1 배포 회고" 항목 추가

위 4개 모두 ✓ 되면 U8 마감, U10 (운영 페이지) 진입.
